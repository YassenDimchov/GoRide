# GoRide
GoRide is a ride-sharing platform that connects passengers and drivers through a modern web application. It is built with Laravel (PHP) for the backend, a relational SQL database for data management, and a responsive frontend to handle the full ride lifecycle.
